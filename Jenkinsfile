pipeline {
    agent any

    environment {
        APP_PID_FILE = 'flask_app.pid'
    }

    stages {
        stage('test') {
            steps {
                script {
                   echo "this is master branch"
                    }
                }
            }
        }

       
  }
  
